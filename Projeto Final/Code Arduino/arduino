void setup() {
  Serial.begin(9600);

}

void loop() {
  int sensor1 = analogRead(A0);
  int sensor2 = analogRead(A1);
  int sensor3 = analogRead(A3);
  char text[40];

  sprintf(text, "%d,%d,%d", sensor1,sensor2,sensor3);
  Serial.println(text);
  delay(10);
}
